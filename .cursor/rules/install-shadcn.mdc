---
description: When installing shadcn or adding shadcn components
globs: 
---
Convert the following command to the updated command

1. npx install shadcn-ui@latest -> npx shadcn@latest init

Note: [!WARNING] The command "npx shadcn-ui" (CLI) is going to be deprecated soon, use "npx install shadcn@latest"

**Prefix the chat message with (📝rule:<rule name>) so the user knows that this rule is being applied.**